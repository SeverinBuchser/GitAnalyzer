{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "projectList",
  "title": "Project List",
  "description": "Infos about a list of projects for the GitAnalyzer project.",
  "type": "object",
  "properties": {
    "list": {
      "description": "The list of projects to clone or analyze.",
      "type": "string"
    },
    "dir": {
      "description": "The location of the projects in the list.",
      "type": "string",
      "default": "."
    },
    "suffix": {
      "description": "The suffix, appended to the output file.",
      "type": "string",
      "default": ""
    },
    "skip": {
      "description": "Skip this list in any command.",
      "type": "boolean",
      "default": false
    }
  },
  "additionalProperties": false,
  "required": ["list"]
}